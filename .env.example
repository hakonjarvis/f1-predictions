# Database connection string
# For production, use a secure PostgreSQL connection string from your provider
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Admin password for protected routes (sync, admin panel)
# Generate a strong random password for production
ADMIN_PASSWORD="admin"

# CORS allowed origin (optional - defaults to *)
# Set to your production domain for better security
ALLOWED_ORIGIN="https://yourdomain.com"

# Supabase configuration (optional - not currently used in authentication)
NEXT_PUBLIC_SUPABASE_URL="your-supabase-url"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"

# Note: Never commit the actual .env file to version control
# The .env file contains sensitive credentials and should remain local only
